<%= form_for(@api_listing) do |f| %>
  <% if @api_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_listing.errors.count, "error") %> prohibited this api_listing from being saved:</h2>

      <ul>
      <% @api_listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
